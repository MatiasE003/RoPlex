# AGENTS.md

## Repository State
- This repository contains a Manifest V3 Chrome extension for Roblox Home, user profile, inventory, and game pages on `https://www.roblox.com`.
- `manifest.json` injects only the minimal `core/activation.css` and classic root `content.js` loader at `document_start` on Roblox pages. The loader activates Home replacement mode synchronously only on the recognized Home route, then imports the small isolated route controller at `core/main.js`. The controller loads and caches one shared `shared/styles.css` link on first entry to Home or Profile plus one `profile/styles.css` link on first entry to Profile, and waits for the required styles before mounting. It dynamically imports and caches the Home implementation graph on first entry to Home, or the Profile implementation graph plus its persisted mode controller on first entry to a profile; unrelated routes load neither presentation CSS nor renderer graph. Profile replacement mode is activated only after its persisted preference resolves, its styles load, and the extension root mounts. Those packaged modules and route styles are exposed only to `https://www.roblox.com/*` through `web_accessible_resources` because Chrome does not support module declarations for manifest content scripts. The route controller remains loaded on other Roblox routes so same-document navigation into Home or a profile can mount exactly one matching application root.
- The Home bootstrap reads the authenticated account from `users.roblox.com`, the user's Robux balance from `economy.roblox.com`, friends from `friends.roblox.com`, the authenticated user's custom names for those friends from `contacts.roblox.com`, presence from `presence.roblox.com`, circular headshots and game icons from `thumbnails.roblox.com`, and the authenticated Home discovery feed from `apis.roblox.com` through the background service worker.
- Game pages load `server-browser/styles.css` and `server-browser/server-browser.js`; `platform/roblox-page-bridge.js` runs in the page's main world on Roblox pages so it can relay custom-element lifecycle and route changes, while game pages also use it for direct joins through Roblox's official `GameLauncher`.
- Game-page match patterns cover both `/games/*` and locale-prefixed paths such as `/es/games/*`; `https://www.roblox.com/*` is also a host permission because dynamic main-world injection uses `chrome.scripting`.
- `background/service-worker.js` owns Roblox API orchestration, bounded cursor pagination, retry/backoff, and the expiring region/datacenter cache. Each Server Browser run is one operation-ID-scoped `chrome.runtime.Port` session with an `AbortController`: background overlaps pagination with a bounded analysis pool, batches region-cache access per page, and streams result chunks and progress. Region POSTs execute in the Roblox tab's main world so their origin/referrer match the website request; active MAIN-world requests are registered by operation so cancellation can abort them. `GameJoinRegion` is extracted from the JSON stored in `joinScript.SessionId` when Roblox does not return it as a direct property. The public IP from `joinScript.UdmuxEndpoints` is geolocated through `https://ipwho.is`, with request deduplication, rate-limit handling, and separate expiring IP and `DataCenterId` location caches.
- `network-rules.json` contains a single scoped MV3 `declarativeNetRequest` rule that sets Roblox's required `User-Agent` only for `join-game-instance` requests initiated by `www.roblox.com`.
- `inventory/badge-bulk-delete.js` and `inventory/styles.css` run on standard and locale-prefixed user inventory pages. On the authenticated user's own `#!/badges` section, they add a top-right Bulk delete control, turn visible badge cards into a multi-page selection surface, show irreversible-action and progress states, and preserve failed selections when Roblox returns partial failures. The controls remain hidden when ownership cannot be verified and are removed when the user leaves the badges category.
- `background/service-worker.js` validates badge inventory ownership against `users.roblox.com` before destructive requests and handles bounded sequential deletion through `DELETE badges.roblox.com/v1/user/badges/{badgeId}`. It obtains and reuses Roblox's `X-CSRF-TOKEN` from the initial `403`, retries transient failures with backoff, and reports per-badge success or failure to the inventory UI.
- The `home/` modules replace Roblox Home with the first production slice of the adaptable Home design. `core/main.js` owns eager route coordination and lazy renderer registration, `home/home-app.js` owns Home mounting and account bootstrap, `shared/app-shell.js` owns the shared shell, `home/friends.js`, `home/game-feed.js`, and `home/home-search.js` own their UI domains, while `shared/config.js`, `shared/messaging.js`, and `shared/ui.js` contain shared contracts and helpers. They request `GET_HOME_BOOTSTRAP`, `GET_HOME_FRIENDS`, `GET_HOME_CONTINUE`, `GET_HOME_FAVORITES`, and `GET_HOME_RECOMMENDED` from `background/service-worker.js`, render the authenticated user's identity and avatar, update account-specific links, and expose independent loading, error, and retry states. Friends are sorted by presence and the first seven are shown. Hovering or focusing a friend lazily requests `GET_HOME_FRIEND_PREVIEW`, which renders social counts, join date, activity, profile navigation, and an official direct Join action when Roblox exposes a public game instance. The top-bar search debounces `SEARCH_HOME_USERS` requests as the user types, displays up to eight matching profiles with direct profile links, and offers searches for players, experiences, communities, or Roblox documentation. Continue renders up to 30 experiences in Roblox's feed order, Favorites renders up to 30 publicly accessible favorites in Roblox's favorite order, and Recommended renders up to 50 valid personalized results returned by Roblox as a four-column landscape grid. All game cards show a thumbnail, rating, live player count, and direct game navigation. Vertical mouse-wheel input over either horizontal carousel moves it sideways and remains contained at either edge instead of scrolling the page. Continue and Recommended share the same authenticated discovery response through a 30-second background cache.
- `shared/routes.js` recognizes Home and user profile routes, `shared/app-shell.js` owns the shared topbar/sidebar shell, and `profile/profile-app.js` plus `profile/profile-sections.js` replace profile pages with independent loading, error, and retry states. The profile bootstrap and Avatar start immediately, while one root-owned `IntersectionObserver` requests Creations, Favorites, Friends, Communities, and Badges shortly before they approach the viewport; profile anchors trigger their target immediately. The profile bootstrap resolves the target identity, authenticated viewer, viewer-specific custom nickname, social counts/status, and presence. Separate requests load every returned equipped avatar asset and emote with its public Robux price when available, up to 50 public creations, up to 50 favorites, all public friends, all communities, and the 16 most recent badges. Friends and communities append in bounded scheduled batches with lazy, asynchronous image decoding. Equipped avatar items render in a two-row horizontal carousel controlled by wheel, trackpad, or scrollbar. The page renders the remaining sections in one vertical flow without `See all` navigation; bounded results explicitly report truncation. Profile reads use bounded memory TTL caches, exact in-flight deduplication, shared full-avatar and per-universe game enrichment, and authenticated-viewer partitioning for viewer-specific results.
- `profile/profile-mode.js` owns the persisted `chrome.storage.local` preference and the fixed profile-mode switch. The switch remains available in both modes: disabling it unmounts the extension profile and restores Roblox's original page, while enabling it remounts the redesign without a reload. Preference changes propagate to other open Roblox tabs through `chrome.storage.onChanged`.
- Profile headers use the authenticated viewer's custom nickname when one exists while retaining the canonical display name and username. Own profiles and signed-out views omit Add Friend, Message, and Follow. Other signed-in profiles use official friendship/follow-state checks, a real message link, and CSRF-protected friend/follow actions; existing friends do not receive an Add Friend control.
- `core/activation.css` owns the global Home/Profile activation and anti-flash selectors. Route-loaded `shared/styles.css` contains responsive shared-shell presentation scoped to the extension roots, while route-loaded `profile/styles.css` contains Profile and profile-mode-switch styling scoped to their roots. Friend activity labels use blue for Online/Studio, green for a visible game name or privacy-limited Playing state, and muted gray for Offline.
- `server-browser/server-browser.js` detects standard and locale-prefixed game PlaceIds, requires the user to choose whether Roblox should return fuller or emptier servers first, starts one background-owned analysis session, and supports explicit cancellation. It maintains keyed group/card nodes, appends or updates only changed live results, keeps Join state outside the DOM, and performs a final ordering pass at completion. Approximate locations come from the public UDMUX endpoint IP; compact labels prefer the administrative region over the city (for example, `São Paulo, Brasil`). Its global DOM observer exists only while locating Roblox's mount anchor, then a bounded lifecycle observer watches anchor replacement and disconnects outside the game-instances route.
- `server-browser/styles.css` contains all Server Browser styling and follows Roblox's light/dark themes.
- There is no repo-local package manager, build system, linter, typechecker, CI config, or bundled entrypoint. Server Browser pipeline regression tests use Node's built-in test runner.

## Performance Optimization TODOs
- These TODOs record the August 2026 static performance audit. Future agents should validate the affected behavior and current code before editing, but should not need to repeat the repository-wide audit. Preserve independent loading/error states, route-aware same-document navigation, authenticated-account isolation, destructive-action safeguards, and the documented manual behavior while implementing them.

### P0 - Route Loading and Startup
- [x] **Lazy-load Home and Profile implementation modules by route.** `core/main.js` now statically imports only route/event contracts, caches independent dynamic import promises for the Home and Profile implementation graphs, and registers each renderer set only on first entry to its route. The Profile import also owns the deferred `profile/profile-mode.js` preference watcher. Every async completion revalidates the current pathname before mounting, preserving direct and same-document navigation without stale or duplicate roots while unrelated routes evaluate neither renderer graph.
- [x] **Load route-specific CSS only when needed.** `manifest.json` now injects only `core/activation.css` at `document_start`. `core/main.js` starts and caches idempotent shared/Profile stylesheet requests by route, awaits the required links before mounting, retries failed links, and revalidates the route before activation. The global activation sheet prevents native Home flashes, while Profile remains native until its preference, renderer, and styles are ready. Shared presentation selectors are scoped to the extension roots, and unrelated direct routes load neither presentation stylesheet.
- [x] **Remove the Home startup dependency on the Profile preference.** `core/main.js::initializeRouteController()` currently waits for `getProfileRedesignEnabled()` before reconciling every route. Home does not need this preference. Reconcile and mount Home as soon as `document.body` exists; resolve the preference only for Profile. Do not hide a profile until the preference is known and an extension root can be mounted. Done when Home startup is not blocked by `chrome.storage.local`, a disabled Profile never briefly hides Roblox, and import/storage failures restore the native page.

### P0 - Profile Request and Rendering Cost
- [x] **Stage Profile section requests by viewport proximity.** Profile bootstrap and Avatar now start immediately, while one `IntersectionObserver` owned by `ProfileApp` requests the remaining sections within a bounded viewport margin. Per-generation section state deduplicates observer, anchor, and retry triggers; hash-only navigation and initial profile fragments trigger and scroll to their target without weakening stale-response guards.
- [x] **Render large Profile collections incrementally.** Friends and Communities now mount an empty resolved panel and append every returned record in batches of 24 using `DocumentFragment` plus idle/frame scheduling. Each batch checks the active profile generation, and below-fold friend/community images use lazy loading and asynchronous decoding while retaining image fallbacks.
- [x] **Deduplicate and briefly cache Profile reads by target user.** Safe Profile reads now use separate exact in-flight and bounded TTL/LRU maps. Viewer-specific Bootstrap/Friends results are keyed by authenticated viewer and target, revalidated against account changes, and invalidated after relationship mutations; public section results are target-scoped. Full-avatar thumbnails share one target-keyed request/cache across Bootstrap and Avatar, while overlapping Creations/Favorites share per-universe game, vote, and icon enrichment. Mutation results are never cached as reads.

### P0 - Server Browser Pipeline
- [x] **Replace one-message-per-server analysis with one background-owned operation.** Each run now uses one operation-ID-scoped `chrome.runtime.Port`. Background follows cursors until Roblox reports no next page while earlier pages enter a bounded worker pool, deduplicates `${placeId}:${jobId}`, batches region-cache reads and writes per page, and streams progress plus chunks of at most 24 results. The global region limiter and retry policy remain authoritative in background. The deterministic 5,000-server fixture verifies one session, all 50 page/cache batches, 5,000 streamed results, and first-page results before pagination completes.
- [x] **Add real cancellation for long server analyses.** Every operation owns an `AbortController` and is canceled on route exit, refresh/restart, the explicit Cancel action, or Port disconnect. Pagination, rate-limit waits, retries, geolocation, and cache writes check the signal. MAIN-world fetches register page-realm controllers by operation ID, allowing background cancellation to abort already injected requests. Canceled status is non-error UI state and operation IDs prevent stale chunks from entering a later run; the regression fixture verifies that cancellation aborts active MAIN-world work and starts no later region checks.
- [x] **Render live server results incrementally instead of rebuilding the tree.** Server and group state is keyed by JobId/location, live chunks append or update only affected cards, and Join clicks are delegated from the root. Join pending/error state lives in application maps and survives card updates. Filtering and the completion pass reorder existing keyed nodes, while `content-visibility: auto` limits below-fold rendering cost for large collections.
- [x] **Narrow Server Browser DOM observation.** A document-subtree observer runs only while locating `#rbx-public-running-games`; after mounting, a lifecycle observer watches the nearest stable boundary only for anchor/root replacement. Extension-owned mutations and unrelated mutations inside Roblox's server list are ignored, and both observers disconnect outside the game-instances route.

### P1 - Network, Retry, and Cache Infrastructure
- [x] **Introduce a shared abortable HTTP layer with deadlines and per-origin budgets.** Idempotent reads now use per-attempt timeouts that cover body consumption, an overall operation deadline, full-jitter backoff, endpoint/method-specific retry counts, and shared per-origin concurrency/cooldown budgets. Valid `Retry-After` values are honored only within the remaining deadline; longer waits fail fast. Badge and profile mutations retain explicit sequential retry/revalidation policy while using the shared bounded single-attempt primitive.
- [x] **Make caches optional and batch storage access.** Five-minute region entries now use bounded memory plus `chrome.storage.session`, while long-lived IP/datacenter locations remain in `chrome.storage.local`. Reads degrade to misses, writes/removals/cleanup are non-fatal, page region access remains batched, and versioned local/session indexes replace broad `get(null)` cleanup scans.
- [x] **Scope all personalized caches and in-flight requests to the authenticated account.** Home discovery cache and exact in-flight deduplication are keyed by authenticated user, cleared on detected account changes, and revalidated before results enter cache. The existing Profile viewer-scoped reads retain their viewer key, epoch validation, and relationship-mutation invalidation.
- [x] **Bound memory caches and separate stable from volatile data.** Friend previews now keep independent bounded TTL/LRU caches and bounded in-flight maps for stable profile/social data and short-lived game activity. Expired entries are removed on access and concurrent identical requests share work without extending the activity TTL.
- [x] **Persist only lifecycle-critical MV3 throttle state.** Adaptive region rate/cooldown state and geolocation `blockedUntil` are persisted best-effort in `chrome.storage.session` and restored on worker evaluation/startup. Promises, controllers, response caches, and queue state remain memory-only.

### P1 - Inventory and Shared DOM Work
- [ ] **Move bounded badge deletion into one background session.** Inventory currently sends one runtime message per badge and repeats authenticated-user lookup for every deletion. Accept a strictly bounded ID list in one request or Port session, validate sender/ownership once, process deletes sequentially in background, stream per-item progress, preserve failed IDs, and revalidate the account after meaningful delays/auth failures. Keep deletion concurrency at one unless Roblox documents a safe alternative. Done when deleting N badges no longer causes N extension RPC/auth-bootstrap cycles while preserving irreversible-action safeguards and partial-failure behavior.
- [ ] **Narrow Inventory observation and decoration.** `inventory/badge-bulk-delete.js` observes the entire document and rescans visible cards during selection. Observe the inventory route/container and `#assetsItems` only while the badge category is active; decorate only added/changed cards and disconnect elsewhere. Reconciliation itself must clear selection on every invalid route regardless of which navigation event fired. Done when unrelated page mutations do not trigger full card scans and category changes leave no stale selection state.
- [ ] **Reuse hot-path formatters and image primitives after larger work.** Hoist repeated `Intl.NumberFormat` instances in `shared/ui.js`, `shared/app-shell.js`, and `server-browser/server-browser.js`. Consolidate a shared image helper that supports HTTPS validation, error fallback, eager/lazy policy, and async decoding. This is a low-impact cleanup and should not be prioritized over route loading, Profile staging, or Server Browser redesign. Done when render loops allocate no formatter per item and thumbnail failures retain the documented fallback UI.

### P2 - Architecture Required to Sustain the Optimizations
- [ ] **Convert and split the service worker into ES modules by domain.** `background/service-worker.js` currently combines RPC dispatch, Home/Profile orchestration, CSRF mutations, pagination, server-region analysis, geolocation, rate limiting, and cache storage in one file. Change the manifest background entry to `{ "service_worker": "background/main.js", "type": "module" }` and extract modules around existing behavior: `rpc/router`, `rpc/contracts`, `http/roblox-client`, `http/retry-policy`, `home/service`, `profile/service`, `inventory/badge-service`, `servers/public-servers`, `servers/region-analyzer`, `servers/geolocation`, and `servers/cache`. Make the smallest behavior-preserving moves first; do not combine this structural change with altered API semantics without tests. Done when background domains can be syntax/unit tested independently and the listener entrypoint only dispatches validated messages.
- [ ] **Centralize message and operation contracts.** Message strings, error-envelope decoding, event names, page limits, and retry settings are duplicated across `shared/config.js`, `shared/messaging.js`, `server-browser/server-browser.js`, inventory, the bridge, and background. Create a shared/versioned contract source usable by extension-world modules and validate request/response payloads at boundaries. Keep the service worker authoritative for effective limits and return those limits in responses for UI text. Done when callers do not duplicate effective backend limits and unknown message types are rejected before unrelated field parsing.
- [ ] **Add measurement and regression tooling before claiming completion.** Establish a minimal repository-local test setup and CI when implementation begins. Cover route parsing, account-keyed cache isolation, geolocation cache round trips, retry/cancellation, Profile staged loading, and a deterministic 2,000-server fixture. Record request counts, cache hits/misses, canceled operations, DOM node counts, long tasks, and time to first server result. Update the Verification section and this file with exact commands after adding tooling. A performance TODO is complete only when behavior checks pass and measurements show the intended request/DOM reduction rather than only a file-structure change.

### Performance Guardrails
- Do not remove Roblox's hidden native application or suspend its runtime without profiling first; it may own routing, chat, account switching, or launcher behavior. Measure hidden-root mutations, network activity, CPU, and heap growth before attempting an invasive change.
- Do not increase badge deletion concurrency or Roblox join-region request rates as a performance shortcut. Preserve sequential destructive operations and the configured global provider limits.
- Do not introduce a bundler solely for code splitting; native extension ES modules and dynamic imports are sufficient unless a separate measured need appears.
- Prefer the smallest independently verifiable optimization. Preserve stale-response guards until real cancellation is proven end to end.

## Design Prototypes
- `ideas front/` is only a container for static design ideas that may guide future extension changes. Files in this directory are references, are not production extension entrypoints, and must not be treated as implemented functionality.
- `ideas front/adaptable-home.html` is the selected design reference for the ongoing replacement of Roblox `/home`; the other files in `ideas front/` remain alternative or historical concepts.
- `ideas front/adaptable-profile.html` is the desktop design reference for the production user profile replacement.
- The adaptable prototypes remain static HTML/CSS references with placeholder data and no application logic. Production behavior belongs in the root `content.js` loader, the feature/shared modules, their scoped stylesheets, and `background/service-worker.js`, and obtains dynamic Roblox data exclusively from Roblox APIs.

## Verification
- Syntax checks:
  - `node --check content.js`
  - `node --check core/main.js`
  - `node --check shared/app-shell.js`
  - `node --check shared/config.js`
  - `node --check shared/messaging.js`
  - `node --check shared/routes.js`
  - `node --check shared/ui.js`
  - `node --check home/friends.js`
  - `node --check home/game-feed.js`
  - `node --check home/home-app.js`
  - `node --check home/home-search.js`
  - `node --check profile/profile-app.js`
  - `node --check profile/profile-mode.js`
  - `node --check profile/profile-sections.js`
  - `node --check terminal/terminal.js`
  - `node --check terminal/terminal-commands.js`
  - `node --check inventory/badge-bulk-delete.js`
  - `node --check background/service-worker.js`
  - `node --check server-browser/server-browser.js`
  - `node --check platform/roblox-page-bridge.js`
- Manifest parse check: `node -e "JSON.parse(require('fs').readFileSync('manifest.json', 'utf8'))"`.
- Network rules parse check: `node -e "JSON.parse(require('fs').readFileSync('network-rules.json', 'utf8'))"`.
- Server Browser pipeline regression and measurement fixture: `node --test tests/server-browser-pipeline.test.js`.
- Network, retry, account-isolation, cache-failure, and throttle-restoration regressions: `node --test tests/network-cache-infrastructure.test.js`.
- Terminal command registration, messaging, and column-format regressions: `node --test tests/terminal-commands.test.js`.
- Manual verification: reload the unpacked extension in Chrome from the repository root, accept the `apis.roblox.com`, `avatar.roblox.com`, `badges.roblox.com`, `users.roblox.com`, `contacts.roblox.com`, `economy.roblox.com`, `friends.roblox.com`, `games.roblox.com`, `groups.roblox.com`, `premiumfeatures.roblox.com`, `presence.roblox.com`, and `thumbnails.roblox.com` host permissions, then refresh `https://www.roblox.com/home` while signed in.
- Expected Home behavior: Roblox's original Home is hidden, the adaptable Home shell renders immediately, and the account card plus sidebar resolve to the authenticated display name, username, avatar, profile, friends, and inventory links. The top bar shows the authenticated user's Robux balance between notifications and settings. Typing at least three characters in the top search returns up to eight matching Roblox profiles beneath the input after a short debounce; each result includes its headshot and opens the profile directly, while separate links search the same text in players, experiences, communities, or the official Roblox documentation. The Friends section resolves up to seven presence-sorted friends with their custom name when one is set, otherwise their Roblox display name, plus their headshot, current activity, and presence indicator. Online/Studio activity text is blue, visible game names and privacy-limited Playing text are green, and Offline remains muted gray. Hover or keyboard focus opens a profile preview; its details load once on demand and are cached for 5 minutes. Playing friends show the experience and Join button when Roblox exposes enough presence data, while private activity receives an explicit privacy message. Continue independently resolves recent experiences, Favorites independently resolves the authenticated user's publicly accessible favorite experiences, and Recommended independently renders up to 50 personalized landscape cards; all include thumbnails, ratings, and live player counts. Scrolling down over either carousel advances right, scrolling up advances left, and further wheel input at either horizontal edge is consumed without moving the page. Account, Friends, search, Continue, Favorites, Recommended, and preview failures have separate states; missing thumbnails fall back without failing their sections.
- Profile verification: open `https://www.roblox.com/users/{authenticatedUserId}/profile`, confirm Roblox's original profile is hidden, dynamic profile sections load independently, all equipped avatar assets/emotes are visible in a two-row horizontal carousel with public Robux prices when available, vertical wheel input moves that carousel sideways, exactly 16 recent badges render, and Add Friend, Message, and Follow are absent. Disable the `Profile redesign` switch and confirm the extension root is removed while Roblox's original profile and the switch remain visible; enable it again and confirm the redesign remounts without reloading. Open another user's profile and confirm a viewer-specific custom nickname becomes the primary name when present while the canonical display name and username remain visible. Confirm existing friends do not show Add Friend, non-friends expose the appropriate social controls, every returned friend/community renders in the vertical flow, partial failures can retry independently, and direct plus History API navigation between Home and multiple profiles leaves exactly one extension root mounted.
- Badge inventory verification: open `https://www.roblox.com/users/{authenticatedUserId}/inventory/#!/badges`, confirm Bulk delete appears at the upper right only for the authenticated user's inventory, enter selection mode, select and deselect badge cards without navigating, and confirm the selected count updates. Confirm deletion and verify successful cards are removed, partial failures remain selected with an error, and a fully successful batch reloads the inventory. Switch categories and confirm the controls and selections disappear. Open another user's badge inventory and confirm the controls are absent.
- Game-page verification: open `https://www.roblox.com/games/{placeId}/...`, confirm Server Browser appears above Roblox's server list without starting a request, choose fuller or emptier servers first, and confirm first-page results can render while later pages are still loading. Confirm live progress advances in the chosen player-count order, servers from different `DataCenterId` values in the same geolocated administrative region render together under a compact region/country label, cards show only players and FPS, filters reorder results, and `Entrar` opens the selected JobId. While loading, cancel and confirm the partial results remain with a non-error canceled state; start again, navigate away, and confirm authenticated region checks stop without stale updates when returning.
- The Server Browser follows Roblox's public-server cursor until no next page remains, without a page-count limit, and uses 20 concurrent join-game workers. Repeated cursors are rejected to prevent pagination loops; edit `SERVER_BROWSER_CONFIG.analysisConcurrentRequests` in `background/service-worker.js` to tune worker concurrency.
- Region checks run at a maximum of 8 requests per second globally, including retries. A Roblox `429` automatically reduces the rate by half, down to a minimum of 5 requests per second, observes a bounded `Retry-After` cooldown, and recovers gradually up to 8; edit the `region*` values in `SERVER_BROWSER_CONFIG` in `background/service-worker.js` to tune this behavior.
- Region/datacenter cache entries expire after 5 minutes; edit `SERVER_BROWSER_CONFIG.cacheTtlMs` in `background/service-worker.js` to tune the TTL.
- IP geolocation checks are limited to 10 requests per second, deduplicated by address and `DataCenterId`, and cached for 30 days; edit the `geolocation*` values in `SERVER_BROWSER_CONFIG` in `background/service-worker.js` to tune these limits. Temporary lookup failures are negatively cached for 10 minutes, and provider `429` responses pause further lookups according to `Retry-After`.
- There is still no repo-local package manager, build, lint, typecheck, or CI config. The repository-local regression tests use Node's built-in test runner with the exact commands above.
- After adding further executable config, update this file with the exact commands from manifests, task runners, or CI.
